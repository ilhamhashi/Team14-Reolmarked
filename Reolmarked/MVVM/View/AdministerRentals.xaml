<UserControl x:Class="Reolmarked.MVVM.View.AdministerRentals"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Reolmarked.MVVM.View"
        xmlns:vm="clr-namespace:Reolmarked.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="AdministerRentals" Height="450" Width="800">
    
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#a2bcd9"/>
            <Setter Property="Foreground" Value="#070808"/>
            <Setter Property="FontSize" Value="12"/>
            
        </Style>
    </UserControl.Resources>

    <Grid>
        <Button Name="TerminateBtn" Content="Opsig lejeaftale" 
                Height="25" Width="110" Margin="600 350 0 0"
                Command="{Binding Path=TerminateRentalCommand,
                UpdateSourceTrigger=PropertyChanged}">
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="10"/>
                </Style>
            </Button.Resources>
        </Button>

        <StackPanel>
            <DatePicker x:Name="datePicker" Width="100" Height="50" 
                        >
                <DatePicker.Style>
                    <Style TargetType="DatePicker">
                        <Setter Property="IsDropDownOpen" Value="False"></Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="IsDropDownOpen" Value="True"></Setter>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter Property="IsDropDownOpen" Value="False"></Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DatePicker.Style>
            </DatePicker>
        </StackPanel>

    </Grid>
</UserControl>
