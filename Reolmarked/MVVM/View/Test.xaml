<UserControl x:Class="Reolmarked.MVVM.View.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.MVVM.View"
             xmlns:vm="clr-namespace:Reolmarked.MVVM.ViewModel" 
             d:DataContext="{d:DesignInstance Type=vm:RentalAgreementViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">

    <UserControl.DataContext>
        <vm:RentalAgreementViewModel/>
    </UserControl.DataContext>

        <Grid>
        <TextBlock Width="250" Height="25"
                   Foreground="Black" FontSize="16">
            <TextBlock.Text>
                <Binding Path="ShelvesColloectionView.Count" StringFormat="Antal reoler: {0}"/>
            </TextBlock.Text>
        </TextBlock>

        <TextBlock Text="Hej"
                   Width="250" Height="25"
                   VerticalAlignment="Bottom"
                   Margin="0 0 0 280"
                   FontSize="16">
            <TextBlock.ControlTemplate>
                <ControlTemplate>
                    <Border Background="LightGray" CornerRadius="5" Padding="5">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </TextBlock.ControlTemplate>
        </TextBlock>
    </Grid>
</UserControl>
