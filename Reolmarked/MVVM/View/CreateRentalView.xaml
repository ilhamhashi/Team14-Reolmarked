<UserControl x:Class="Reolmarked.MVVM.View.CreateRentalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.MVVM.View"
             xmlns:vm="clr-namespace:Reolmarked.MVVM.ViewModel"
             xmlns:controls="clr-namespace:Reolmarked.MVVM.View.controls"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1000"
             Background="#f4f6f8">

    <UserControl.DataContext>
        <vm:CreateRentalViewModel/>
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <GroupBox Header="Opret udlejning">
            <StackPanel>
                <local:RenterSelectionView/>


                <StackPanel Orientation="Horizontal" Grid.Row="2">
                    <Label Content="Valgt reol: "
                       Margin="0,0,10,0"/>
                    <Label Content="{Binding SelectedShelf.ShelfId, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <local:NewRentalSummaryView/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Column="1" Header="Vælg Reol">
            <local:ShelfSelectionView/>
        </GroupBox>

        <!-- 
        <GroupBox Grid.Column="1" Header="Vælg Reol">
            <local:ShelfSelectionView/>
        </GroupBox>
        <GroupBox Grid.Row="1" Header="Reoludlejninger">
            <local:NewRentalSummaryView/>
        </GroupBox>
                <StackPanel Orientation="Horizontal" Grid.Row="3">
                    <TextBlock Text="Tilføj flere reoler"
                       Margin=" 0,0,20,0"/>
                    <CheckBox  Style="{StaticResource SwitchCheckBoxStyle}"
                       IsChecked="False"
                       Command="{Binding IsAddingMoreShelvesCommand, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>

                <Button Grid.Row="4" 
                Content="Bekræft valg"
                Command="{Binding SaveShelfSelectionCommand, UpdateSourceTrigger=PropertyChanged}"
                Margin="0,10,0,0"/>

<ContentControl Margin="10"
                        Content="{Binding CurrentCreateRentalView}"/>

        <StackPanel x:Name="stckPanel" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button Content="Annuller"
                    Command="{Binding TerminateRentalCreationCommand, UpdateSourceTrigger=PropertyChanged}"
                    Margin="10"/>

            <Button Content="Videre"
                    Command="{Binding ContinueCreatingRentalCommand, UpdateSourceTrigger=PropertyChanged}"
                    Margin="10"/>
        </StackPanel> -->
        
    </Grid>
</UserControl> 
