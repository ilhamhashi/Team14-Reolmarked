<UserControl x:Class="Reolmarked.MVVM.View.RenterSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Reolmarked.MVVM.View" 
             xmlns:vm="clr-namespace:Reolmarked.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <StackPanel>
            <Label Content="Vælg en lejer"/>
            <!-- Dropdown til lejere -->
            <ComboBox ItemsSource="{Binding RentersCollectionView, UpdateSourceTrigger=PropertyChanged}"
                      SelectedItem="{Binding SelectedRenter, UpdateSourceTrigger=PropertyChanged}" 
                      >
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <TextBlock.Text>
                                <MultiBinding StringFormat="{}{0}: {1} {2}">
                                    <Binding Path="PersonId"/>
                                    <Binding Path="FirstName"/>
                                    <Binding Path="LastName"/>
                                </MultiBinding>
                            </TextBlock.Text>
                        </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock>
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}Telefon: {0}, E-mail: {1}">
                        <Binding Path="SelectedRenter.Phone"/>
                        <Binding Path="SelectedRenter.Email"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            
            <TextBlock>
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}Adresse: {0}">
                        <Binding Path="SelectedRenter.Address"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <TextBlock>
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}Lejekontraktnr.: {0}">
                        <Binding Path="SelectedRenter.Address"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>

            <Button Grid.Row="4" Content="Gem ny lejekontrakt"
                        Command="{Binding AddRentalCommand, UpdateSourceTrigger=PropertyChanged}"/>

        </StackPanel>
    </Grid>
</UserControl>
