<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="Button" x:Key="myShelfButton">
        <Setter Property="Width" Value="28"/>
        <Setter Property="Height" Value="14"/>
        <Setter Property="Background" Value="#008000"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontFamily" Value="Bodoni MT"/>
        <Setter Property="FontSize" Value="8"/>
        <Setter Property="Padding" Value="5,0"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="BorderBrush" Value="#006400"/>
        <Setter Property="BorderThickness" Value="0.5"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover-effekt -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#6b8e23"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#556b2f"/>
                        </Trigger>
                        <!-- Tryk-effekt -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#f4f6f8"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#000a19"/>
                        </Trigger>
                        <!-- Deaktiveret -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Background" Value="#b22222"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#8b0000"/>
                            <Setter Property="Foreground" Value="#000000"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>